@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ActionName</title>
</head>
<body>
    <div>The controller is: @ViewBag.Controller</div>
    <div>The action is: @ViewBag.Action</div>
    @*<div>
            @Html.ActionLink("This is an outgoing URL", "CustomVariable", new { id = "Hello" })
        </div>
        <div>
            @Html.ActionLink("This targets another controller", "Index", "Customer")
        </div>*@
    @*<div>
            @Html.ActionLink("this is an outgoing URL", "Index", "Home", "https", "myserver.domain.com", " ,myFragmentName", new { id = "MyId" }, new { id = "myAnchorID", @class = "myCSSClass" })
        </div>*@
    <div>
        This is a URL:
        @Url.Action("Index", "Home", new { id = "MyId" })
    </div>
    
    @Html.ActionLink("Click me", "Index", "Customer")
    @Html.RouteLink("Click me", "MyOtherRoute", new { action = "Index", controller = "Customer" })
</body>
</html>

